<%= form_with(model: [@todo_list, todo_item], local: true) do |form| %>
  <div class="input-group mb-3">
    <%= form.text_field :name, class: "form-control", placeholder: "New todo item...", required: true %>
    <%= form.submit "Add", class: "btn btn-success" %>
  </div>
<% end %>
