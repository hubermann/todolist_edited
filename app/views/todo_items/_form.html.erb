<%= form_with(model: [todo_list, todo_list.todo_items.new]) do |form| %>
  <div class="input-group mb-3">
    <%= form.text_field :name, class: "form-control", placeholder: "New todo item..." %>
    <%= form.submit "Create", class: "btn btn-success" %>
  </div>
<% end %>
